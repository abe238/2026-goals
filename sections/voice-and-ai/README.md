# Section README

See the corresponding milestone instruction file in `product-plan/instructions/incremental/` for full details on this section.

## Components

See `components/` directory for React components.

## Types

See `types.ts` for TypeScript interfaces.

## Sample Data

See `sample-data.json` for test data.
